# Normalize all text files to LF in the repository (repo norm is LF).
# Prevents CRLF blobs when committing from a Windows working tree.
* text=auto

# Explicit binaries (never touch line endings).
*.png binary
*.jpg binary
*.ico binary
